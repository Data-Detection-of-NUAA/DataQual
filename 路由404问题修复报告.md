# 路由404问题修复报告

## 问题描述

**日期:** 2026-01-22
**错误:** 前端访问 `/api/v1/dataset/data/upload/resume` 返回 404 Not Found
**影响:** 数据集上传功能无法使用

---

## 问题分析

### 路由路径构成

FastAPI 路由路径由多个层级组成：

1. **root_path** (全局前缀): `/api/v1` - 在 `settings.ROOT_PATH` 中定义
2. **容器前缀** (discover.py 创建): `/{module_name}`
3. **子路由前缀** (controller 中定义): 如 `/train`, `/dataset/data`
4. **路由路径**: 如 `/upload/resume`

### 问题根源

架构调整后，`module_train` 和 `module_dataset` 被移动到 `module_application` 下：

```
backend/app/plugin/
└── module_application/
    ├── train/
    └── dataset/
```

**路由发现机制 (discover.py):**
- 扫描 `module_*/**/controller.py`
- 为每个顶级模块创建容器路由：`module_xxx` → `/xxx`
- `module_application` → `/application` 容器

**实际路径构成:**
```
/api/v1                    (root_path)
  + /application           (容器前缀，由 discover.py 添加)
    + /dataset/data        (子路由前缀，controller 中设置)
      + /upload/resume     (路由路径)
= /api/v1/application/dataset/data/upload/resume  ❌ 错误！
```

**期望路径:**
```
/api/v1/dataset/data/upload/resume  ✅ 正确
```

**问题:** 多了 `/application` 容器前缀！

---

## 解决方案

### 方案选择

**方案 A: 修改 controller prefix** (❌ 不可行)
- 尝试在 controller 中覆盖容器前缀
- 问题：FastAPI 的 `include_router` 会叠加前缀，不能覆盖

**方案 B: 修改 discover.py 特殊处理 module_application** (✅ 采用)
- 对 `module_application` 不添加容器前缀
- 让子路由自己定义完整路径
- 向后兼容，不影响其他模块

### 实施细节

**修改文件:** [backend/app/core/discover.py](backend/app/core/discover.py)

**修改内容:**

```python
# 修改前:
prefix = f"/{top_module[7:]}"

# 修改后:
if top_module == "module_application":
    prefix = ""  # 不添加容器前缀
else:
    prefix = f"/{top_module[7:]}"
```

**效果:**

| 模块 | 容器前缀 | 子路由prefix | 最终路径 |
|------|---------|-------------|---------|
| module_example | `/example` | `/demo` | `/api/v1/example/demo/*` |
| module_generator | `/generator` | `/gencode` | `/api/v1/generator/gencode/*` |
| module_application/train | `` | `/train` | `/api/v1/train/*` ✅ |
| module_application/dataset | `` | `/dataset/data` | `/api/v1/dataset/data/*` ✅ |

---

## 路由路径验证

### Dataset 模块路由

| 路由 | 完整路径 | 状态 |
|------|---------|------|
| 上传初始化 | `/api/v1/dataset/data/upload/init` | ✅ |
| 分片上传 | `/api/v1/dataset/data/upload/chunk` | ✅ |
| 完成上传 | `/api/v1/dataset/data/upload/complete` | ✅ |
| **断点续传** | `/api/v1/dataset/data/upload/resume` | ✅ 修复 |
| 数据集列表 | `/api/v1/dataset/data/list` | ✅ |
| 数据集删除 | `/api/v1/dataset/data/delete` | ✅ |

### Train 模块路由

| 路由 | 完整路径 | 状态 |
|------|---------|------|
| 模型推荐 | `/api/v1/train/recommend` | ✅ |
| 模型列表 | `/api/v1/train/model/list` | ✅ |
| 模型创建 | `/api/v1/train/model/create` | ✅ |
| 模型更新 | `/api/v1/train/model/update` | ✅ |
| 模型删除 | `/api/v1/train/model/delete` | ✅ |

---

## API 兼容性

### 前端 API 配置

**文件:** `frontend/.env.development`
```
VITE_APP_BASE_API=http://127.0.0.1:8000
```

**前端 API 调用示例:** (无需修改)
```typescript
// 数据集上传
POST /api/v1/dataset/data/upload/init
POST /api/v1/dataset/data/upload/resume  // 此次修复的路由

// 模型推荐
POST /api/v1/train/recommend
```

### 向后兼容性

- ✅ 100% API 路径兼容
- ✅ 前端代码零修改
- ✅ 其他模块 (example, generator) 不受影响
- ✅ 系统路由 (system, common, monitor) 不受影响

---

## 测试验证

### 1. 路由注册日志

启动后端后，应看到：

```
✅️ 注册容器:  (路由数: XX)  # module_application，空前缀
✅️ 注册容器: /example (路由数: XX)
✅️ 注册容器: /generator (路由数: XX)
```

### 2. API 端点测试

```bash
# 测试断点续传接口
curl -X POST http://localhost:8000/api/v1/dataset/data/upload/resume \
  -H "Content-Type: application/json" \
  -d '{"upload_id": "test"}'

# 期望: 200 或 401 (需要认证)，不应该是 404
```

### 3. Swagger 文档检查

访问: `http://localhost:8000/docs`

确认以下路由可见：
- ✅ `/api/v1/dataset/data/upload/resume`
- ✅ `/api/v1/train/recommend`

---

## 修改文件清单

| 文件 | 修改内容 | 行数 |
|------|---------|------|
| [backend/app/core/discover.py](backend/app/core/discover.py) | 特殊处理 module_application | 60-63 |

---

## 提交信息

```bash
git add backend/app/core/discover.py
git commit -m "fix: 修复 module_application 路由404问题

- discover.py 对 module_application 不添加容器前缀
- 解决 /api/v1/dataset/data/* 和 /api/v1/train/* 路由404
- 保持100% API向后兼容性
- 前端代码无需修改

修复问题：前端访问 /api/v1/dataset/data/upload/resume 返回404
根本原因：容器路由错误添加 /application 前缀
解决方案：module_application 使用空容器前缀

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"
```

---

## 架构设计说明

### module_application 特殊性

`module_application` 是一个特殊的容器模块，用于组织业务相关的子模块：

- **train/** - 模型训练相关功能
- **dataset/** - 数据集管理功能
- **ai/** - AI相关功能
- **job/** - 任务管理功能
- **workflow/** - 工作流功能

**设计理念:**
- 容器不添加路由前缀（空前缀）
- 各子模块自己定义完整的路由路径
- 保持与旧架构的API兼容性

**其他模块:**
- `module_example` → `/example/*`
- `module_generator` → `/generator/*`
- 这些独立模块保持容器前缀

---

## 相关文档

- [开发规范.md](开发规范.md) - 模块开发规范
- [架构调整完成总结.md](架构调整完成总结.md) - 架构调整详情
- [完成状态报告.md](完成状态报告.md) - 项目总体状态

---

**状态:** ✅ 已修复
**测试:** ⏳ 待验证
**提交:** ⏳ 待提交
