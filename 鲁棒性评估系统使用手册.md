# 软件使用手册

***

## 1. 软件简介

高质量数据集质量测评系统是为 **AI+智能终端** 打造的 **全生命周期数据质量评估平台**，旨在解决数据缺失、标注噪声、分布偏移等核心问题。系统集成数据集质量探测引擎、多模态鲁棒性评估平台、质量溯源与缺陷分析系统等模块，支持图像、语音、传感器数据等20+模态的自动化质量评估。
- 系统总体界面图：![](assets/1.1.png)


## 3. 多模态鲁棒性评估平台

- **功能简介**：作为高质量数据集质量测评系统的安全防御中枢，直接分析质量探测引擎输出的数据特征，识别对抗脆弱性；并将结果反馈至质量溯源系统定位缺陷根源，形成“检测-防御-溯源”闭环。

- **流程说明**
  - 第一步：模型自适应训练。
  - 第二步：针对模型和数据集选择对应检测鲁棒性方法，并进行鲁棒性评估。
  
- **界面展示**

  - **第一步：**上传数据集。![](assets/3.1.png)

    **功能：10GB+数据集、断点续传**

    - 1.上传初始化

      ​	前端选择文件→调用大文件分片上传系统初始化接口（文件名、大小、用户ID、hash）→后端验证（文件是否存在、存储配额检查、分片信息、upload_id）→返回upload_id、分片列表、存储路径

    - 2.分片上传

      ​	对于每个分片：前端读取分片数据→调用分片上传处理接口（upload_id、分片索引、分片数据、分片hash）→后端存储分片并更新上传进度

    - 3.合并与验证

      ​	所有分片上传完成→调用分片合并接口→后端按序合并分片并移动到正式存储路径，记录到数据库→返回dataset_id、存储路径、文件信息

    - 4.断点续传实现

      ​	每次上传前检查断点续传接口（输入文件hash，返回已上传分片索引列表）→前端只上传缺失的分片，意外中断后自动识别已上传部分

    **数据集自动分析**

    - 界面操作步骤：上传后自动跳转到“数据集分析”页面，显示分析进度动画，分析完成后显示结果面板（模态类型、样本数、文件大小、前五个样本预览等）
    - 前端执行：调用分析接口触发分析→轮询分析状态→渲染分析结果到各个可视化组件
    - 后端执行：触发分析任务（参数{temp_id、analysis_options}、启动分析任务，返回任务ID）→执行分析算法【模态识别（文件头检测+内容采样分析）、样本统计、格式检测】

  - **第二步：**选择推荐的模型。![](assets/3.2.png)

    - 前端：调用模型推荐接口获取推荐→根据用户选择动态更新表单→调用配置接口保存配置
    - 后端实现推荐算法逻辑【输入：数据集分析结果】：模态匹配→规模适配→返回候选模型

  - **第三步：**设置训练网络结构和训练参数。![](assets/3.3.png)

    1. 前端请求推荐配置

       - 前端：获取数据集分析结果，提取必要特征（模态、样本数、类别数）；调用推荐API接口

       - 后端：接收请求，执行推荐算法

         ```
         //层数推荐规则
         IF 样本数 < 10000:
             IF 类别数 < 5: 推荐层数 = 1  # 简单任务用浅层网络
             ELSE: 推荐层数 = 2          # 稍复杂任务用2层
         
         ELSE IF 样本数 < 100000:
             IF 特征维度 > 1000: 推荐层数 = 3  # 高维特征需要更深网络
             ELSE: 推荐层数 = 2
         
         ELSE: 推荐层数 = 3  # 大数据集可用更深网络
         
         //学习率推荐逻辑
         
         基准学习率 = 0.001  # 默认基准
         
         # 根据优化器调整
         IF 优化器 == "sgd":
             学习率 = 基准学习率 × 10  # SGD需要更大学习率
         ELSE IF 优化器 == "adam":
             学习率 = 基准学习率      # Adam用默认值
         ELSE IF 优化器 == "rmsprop":
             学习率 = 基准学习率 × 2  # RMSprop中等
         
         # 根据批次大小调整
         IF 批次大小 > 128:
             学习率 = 学习率 × 2      # 大批次可增大学习率
         ELSE IF 批次大小 < 32:
             学习率 = 学习率 ÷ 2      # 小批次减小学习率
         
         # 根据网络深度调整
         IF 层数 == 3:
             学习率 = 学习率 × 0.8    # 深层网络学习率小些
         ELSE IF 层数 == 1:
             学习率 = 学习率 × 1.2    # 浅层网络学习率可大些
         
         # 限制在合理范围 [0.0001, 0.01]
         学习率 = max(min(学习率, 0.01), 0.0001)
         //批次大小推荐规则
         IF 样本数 < 1000:
             建议批次 = 16
         ELSE IF 样本数 < 10000:
             建议批次 = 32
         ELSE IF 样本数 < 100000:
             建议批次 = 64
         ELSE:
             建议批次 = 128
             
         //训练轮数推荐规则
         
         IF 样本数 < 5000:
             基准轮数 = 100  # 小数据集易过拟合，需更多轮次
         ELSE IF 样本数 < 20000:
             基准轮数 = 80
         ELSE IF 样本数 < 100000:
             基准轮数 = 50
         ELSE IF 样本数 < 500000:
             基准轮数 = 30
         ELSE:
             基准轮数 = 20
         
         # 根据任务复杂度调整（类别数反映复杂度）
         IF 类别数 > 20:
             轮数 = 基准轮数 × 1.5  # 多分类任务需要更多训练
         ELSE IF 类别数 < 3:
             轮数 = 基准轮数 × 0.8  # 二分类任务可减少轮数
         ELSE:
             轮数 = 基准轮数
         
         # 根据网络复杂度调整
         IF 层数 == 3:
             轮数 = 轮数 × 1.2  # 深层网络需要更多训练
         ELSE IF 层数 == 1:
             轮数 = 轮数 × 0.8  # 浅层网络训练更快
         
         # 限制范围 [10, 200]
         轮数 = max(min(round(轮数), 200), 10)
         
         //优化器推荐规则
         
         # 默认推荐Adam（最稳定）
         优化器 = "adam"
         
         # 特殊情况的推荐调整
         IF 样本数 > 100000:
             优化器 = "sgd"  # 大数据集用SGD可能获得更好泛化
         
         IF 模态 == "text" AND 样本数 > 50000:
             优化器 = "adamw"  # 文本任务常用AdamW
         
         IF GPU内存_GB < 4:
             优化器 = "rmsprop"  # 低内存设备用RMSprop更稳定
         
         # 简单任务的优化器
         IF 样本数 < 1000 AND 类别数 < 5:
             优化器 = "sgd"  # 简单任务用SGD足够
         ```

    2. 推荐配置自动应用
       - 前端：自动填充参数表单，显示推荐理由
    3. 用户参数调整与实时验证

  - **第四步：**开始训练。![](assets/3.4.png)

    - 前端：调用创建任务接口创建训练任务，传递完整配置模型参数，接收任务ID并跳转到监控页面

    - 后端：

      1. 任务创建逻辑：输入数据集ID和模型选择与参数，生成任务ID并返回，船舰训练任务记录，准备训练环境

      2. 任务记录结构：{
           "task_id": "TRAIN_1710835200_1001",
           "user_id": 1001,
           "status": "created",

           "dataset_info": {
             "temp_id": "TEMP_1710835200_1001_abc123",
             "filename": "cifar10.zip",
             "modality": "image",
             "sample_count": 50000
           },

           "model_config": {
             "name": "ResNet-18",
             "parameters": {...},
             "hyperparameters": {...}
           },

           "timestamps": {
             "created": "...",
             "estimated_start": "...",
             "estimated_completion": "..."
           }
         }

    **模型训练与监控**

    - 前端：建立**WebSocket连接**，接收实时训练指标和日志，轮询训练状态

    - 后端：

      1. 训练执行引擎：输入数据集路径和训练配置，训练模型后保存
      2. 监控数据流：每个epoch发送一次训练指标（损失、准确率）、实时输出训练日志

    - web Socket实时数据

      ```
      {
        "type": "training_metrics",
        "timestamp": "...",
        "data": {
          "epoch": 12,
          "batch": 150,
          "total_batches": 782,
          
          "metrics": {
            "train_loss": 0.4521,
            "train_accuracy": 0.8732,
            "val_loss": 0.5214,
            "val_accuracy": 0.8456,
            "learning_rate": 0.00087
          },
          
          "progress": {
            "epoch_progress": 0.192,
            "overall_progress": 0.314
          }
        }
      }
      ```

    - 资源监控数据

      ```
      {
        "type": "resource_metrics",
        "data": {
          "gpu": {
            "utilization_percent": 85.3,
            "memory_used_mb": 3245,
            "memory_total_mb": 8192,
            "temperature_c": 72
          },
          "system": {
            "cpu_usage_percent": 45.2,
            "memory_usage_percent": 68.7,
            "disk_io_mb_s": 12.5
          }
        }
      }
      ```

  - **第五步：**模型训练完成并保存。![](assets/3.5.png)

    - 前端：调用结果获取接口得到最终的结果、渲染性能分析图标、提供模型下载功能、生成训练报告PDF
    - 后端：训练完成后计算最终评估指标，生成性能分析报告，保存模型，更新任务状态为completed

  - **第六步：**鲁棒性评估策略选择。![](assets/3.6.png)

  - **第七步：**进行参数设置。![](assets/3.7.png)

  - **第八步：**选择输出参数。![](assets/3.8.png)

  - **第九步：**运行评估。![](assets/3.9.png)

  - **第十步：**查看鲁棒性评估结果。![](assets/3.10.png)

    ![](assets/3.11.png)其中"鲁棒性评估策略选择"模块可以选择上传外部数据集进行测评，其相应流程如下：![](assets/3.12.png)下一步参数配置，设置输出指标。![](assets/3.13.png)

    ![](assets/3.14.png)运行控制。![](assets/3.15.png)最后得到鲁棒性结果。![](assets/3.16.png)

    ![](assets/3.17.png)
