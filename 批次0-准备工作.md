# 批次0：准备工作与备份

> **重要提示：** 由于您之前已经推送过代码，本次调整将直接在 `feature/wxg` 分支上进行，不创建新分支。

---

## ✅ 执行步骤

### 步骤1：确认当前分支状态

```bash
# 1. 查看当前分支
git branch
# 应该显示: * feature/wxg

# 2. 查看工作区状态
git status
```

**如果有未提交的改动：**

```bash
# 提交当前改动
git add .
git commit -m "chore: 架构调整前的代码快照"
git push origin feature/wxg
```

**记录这个commit的hash值（用于回滚）：**

```bash
# 获取当前commit hash
git log -1 --oneline
# 示例输出: 34d66ea feat: 添加dataset_upload功能（移除大文件）
# 记录这个hash: 34d66ea （或完整的commit hash）
```

✏️ **请在这里记录commit hash：** `_________________`

---

### 步骤2：数据库备份（重要！）

#### 方式A：SQLite备份（如果使用SQLite）

```bash
# 查找数据库文件
find . -name "*.db" -o -name "*.sqlite"

# 备份数据库文件
cp backend/fastapi_admin.db backend/fastapi_admin.db.backup_$(date +%Y%m%d_%H%M%S)

# 验证备份文件
ls -lh backend/*.backup_*
```

#### 方式B：MySQL备份（如果使用MySQL）

```bash
# 导出数据库
mysqldump -u root -p fastapi_admin > backup_$(date +%Y%m%d_%H%M%S).sql

# 验证备份文件
ls -lh backup_*.sql
```

#### 方式C：PostgreSQL备份（如果使用PostgreSQL）

```bash
# 导出数据库
pg_dump fastapi_admin > backup_$(date +%Y%m%d_%H%M%S).sql

# 验证备份文件
ls -lh backup_*.sql
```

✅ **备份完成后，请确认：**
- [ ] 备份文件已创建
- [ ] 备份文件大小合理（非空）
- [ ] 备份文件路径已记录

✏️ **备份文件路径：** `_________________`

---

### 步骤3：创建测试检查清单

创建文件 `测试检查清单.md`：

```bash
# 创建测试检查清单文件
cat > 测试检查清单.md << 'EOF'
# 功能测试检查清单

## module_train 功能测试
- [ ] 模型推荐接口 POST /train/recommend
- [ ] 模型列表查询 GET /train/model/list
- [ ] 模型详情查看 GET /train/model/detail/{id}
- [ ] 创建模型配置 POST /train/model/create
- [ ] 更新模型配置 PUT /train/model/update/{id}
- [ ] 删除模型配置 DELETE /train/model/delete
- [ ] 模型统计信息 GET /train/model/statistics
- [ ] 热门模型查询 GET /train/model/popular
- [ ] 按模态查询 GET /train/model/by-modality
- [ ] 按任务类型查询 GET /train/model/by-task
- [ ] 按框架查询 GET /train/model/by-framework
- [ ] 按标签搜索 GET /train/model/search-by-tag

## module_dataset 功能测试
- [ ] 上传初始化 POST /dataset/data/upload/init
- [ ] 分片上传 POST /dataset/data/upload/chunk
- [ ] 完成上传 POST /dataset/data/upload/complete
- [ ] 断点续传 POST /dataset/data/upload/resume
- [ ] 数据集列表 GET /dataset/data/list
- [ ] 数据集详情 GET /dataset/data/detail/{id}
- [ ] 更新数据集 PUT /dataset/data/update/{id}
- [ ] 删除数据集 DELETE /dataset/data/delete
- [ ] 统计信息 GET /dataset/data/statistics
- [ ] 数据集分析 POST /dataset/data/analyze/{id}
- [ ] 分析状态 GET /dataset/data/analyze/status/{id}
- [ ] 数据集预览 GET /dataset/data/preview/{id}

## 前端功能测试
- [ ] 模型推荐页面加载
- [ ] 模型列表页面加载
- [ ] 数据集上传功能
- [ ] 数据集列表页面加载
- [ ] 数据集分析功能

## 性能测试
- [ ] 服务启动时间 < 10秒
- [ ] 路由注册无报错
- [ ] API响应时间正常

## 基线记录（调整前）
- 服务启动时间: _______ 秒
- 路由总数: _______
- /train 路由数: _______
- /dataset/data 路由数: _______
EOF
```

---

### 步骤4：记录当前路由状态（基线）

```bash
# 1. 启动后端服务
cd backend
python main.py

# 观察控制台输出，记录：
# - 服务启动时间
# - 路由注册信息
```

**在浏览器中访问：**
- 打开 `http://localhost:8000/docs`
- 展开所有路由分组
- 截图保存（命名为：`路由状态-调整前.png`）

**记录基线数据：**

✏️ 填写到 `测试检查清单.md` 的"基线记录"部分：
- 服务启动时间: `_______` 秒
- 路由总数: `_______`
- `/train` 路由数: `_______`
- `/dataset/data` 路由数: `_______`

---

### 步骤5：验证当前功能正常

```bash
# 快速测试关键接口

# 1. 测试模型列表（需要登录token）
curl http://localhost:8000/train/model/list?page_no=1&page_size=10 \
  -H "Authorization: Bearer YOUR_TOKEN"

# 2. 测试数据集列表
curl http://localhost:8000/dataset/data/list?page_no=1&page_size=10 \
  -H "Authorization: Bearer YOUR_TOKEN"
```

**或者通过前端测试：**
- [ ] 打开前端应用
- [ ] 登录系统
- [ ] 访问模型管理页面，能正常加载
- [ ] 访问数据集管理页面，能正常加载

---

## ✅ 批次0完成检查清单

在继续批次1之前，请确认：

- [ ] Git工作区干净，当前在 `feature/wxg` 分支
- [ ] 已记录当前commit hash（用于回滚）
- [ ] 数据库已备份，备份文件存在且非空
- [ ] 已创建 `测试检查清单.md` 文件
- [ ] 已记录当前路由基线数据
- [ ] 已保存路由截图
- [ ] 当前系统功能完全正常

---

## 🔄 如何回滚到批次0的状态

如果后续出现问题，执行以下命令回滚：

```bash
# 1. 回滚代码到架构调整前
git reset --hard <您记录的commit-hash>

# 2. 如果已推送，强制推送
git push -f origin feature/wxg

# 3. 恢复数据库（如果需要）
cp backend/fastapi_admin.db.backup_YYYYMMDD_HHMMSS backend/fastapi_admin.db
```

---

## 📝 批次0执行记录

**执行人：** `_________________`

**执行日期：** `_________________`

**执行结果：**
- [ ] ✅ 成功完成
- [ ] ❌ 遇到问题（描述）：`_________________`

**备注：** `_________________`

---

**完成批次0后，请继续执行批次1。**
