# 多算法模块开发规范

统一约定：**一个算法模块 = 前端一个页面 + 后端一个插件模块 + 引擎里一个算法入口**。

---


## 1) 算法模块（引擎）

写在：`/`（与 `dqscan/` 平级；`dqscan/` 只是其中一个算法模块）

内部组织不强制一模一样（因为每个算法模块形态可能不同）

---

## 2) 后端模块

写在：`backend/app/plugin/module_application/<模块名>/`（每个算法模块在后端组织为一个插件）

文件拆分（推荐默认模板；按模块需要增删，不是“一刀切固定”）：
- **建议必备三件套**
  - `controller.py`：HTTP 路由（只做参数接收/调用 service/返回响应）
  - `schema.py`：Pydantic 模型（请求/响应契约）
  - `service.py`：业务编排（任务创建、落盘、路径安全、调用引擎、必要时调用持久化层）
- **按需增加**
  - `ws.py`：WebSocket（需要实时日志/进度才加）
  - `model.py` / `models.py`：ORM 模型（需要数据库持久化才加）
  - `repository.py` / `crud.py`：数据库读写封装（service 调用它；不要把 SQL/ORM 细节散落到 controller）

路由注册位置：
- HTTP：动态发现（扫描 `backend/app/plugin/module_*/**/controller.py`）
- WS：在 `backend/app/plugin/init_app.py` 里手动 `include_router`

落盘目录统一：
- 上传：`backend/static/<模块名>/uploads/`
- 任务：`backend/static/<模块名>/tasks/{task_id}/`（result.json / log.txt / reports/*）

---

## 3) 前端模块

写在：`frontend/src/`

推荐默认模板（建议按这个放；特殊情况可以调整，但要保持职责分离）：
- 页面：`frontend/src/views/module_application/<模块名>/index.vue`
- 页面私有组件（可选）：`frontend/src/views/module_application/<模块名>/components/*`
- API 封装：`frontend/src/api/module_application/<模块名>.ts`
  - 如果该模块接口很多：允许改成目录 `frontend/src/api/module_application/<模块名>/*`（按子域拆文件）
- axios 统一封装（必须唯一入口）：`frontend/src/utils/request.ts`
  - 页面禁止直接 `new axios / fetch`，必须通过 `src/api/**` 调用
- 路由表（当前项目入口）：`frontend/src/router/index.ts`

---

## 4) 建议统一一套“任务型算法模块接口”

为了让每个算法模块页面写法一致，后端每个模块建议都提供同一组接口（）：
- `POST /upload`（可选：需要文件就做）
- `POST /tasks`（创建任务）
- `GET /tasks/{task_id}`（查进度/状态）
- `GET /tasks/{task_id}/result`（读 result.json）
- `GET /tasks/{task_id}/artifact?path=...`（下载报告/产物）
- `GET /tasks/{task_id}/report`（可选：网页“报告视图”读取 reports/*.json）
- `WS  /ws/{task_id}`（实时日志/进度）

这样前端可以复用同一套 UI/逻辑（上传→创建→WS→结果→报告）。

---

## 5) 新增一个算法模块（团队标准步骤）

以 `模块名 = xxx_algo` 为例：

1) 算法模块：新建目录 `<模块名>/`，实现算法入口 + 在 `<模块名>/engine/registry.py` 注册
2) 后端：新建 `backend/app/plugin/module_application/xxx_algo/`，写 `controller/schema/service/ws`
3) 前端：新建 `frontend/src/views/module_application/xxx_algo/index.vue`
4) 前端：新建 `frontend/src/api/module_application/xxx_algo.ts`
5) 前端：在 `frontend/src/router/index.ts` 加 `/<模块名>/index` 路由
6) 约定落盘：`backend/static/xxx_algo/` 下 uploads/tasks 结构保持一致



