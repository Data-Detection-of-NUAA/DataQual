# 批次0基线记录

**记录时间：** 2026-01-22 10:05

---

## Git状态

### 回滚点Commit
- **Commit Hash:** `d9c04781657b9d5db02ee704536dd98be4422b06`
- **短Hash:** `d9c0478`
- **提交信息:** chore: 架构调整前的代码快照
- **提交时间:** 2026-01-22 10:04:36

### 分支信息
- **当前分支:** feature/wxg
- **父分支:** main

---

## 数据库备份

### 备份配置
- **数据库类型:** MySQL
- **数据库名称:** fastapiadmin
- **数据库主机:** localhost:3306
- **备份脚本:** [backup_database.sh](backup_database.sh)

### 备份说明
由于需要手动输入密码，请在执行架构调整前运行：
```bash
./backup_database.sh
```

备份文件将保存为: `database_backup_YYYYMMDD_HHMMSS.sql`

---

## 目录结构（调整前）

### 后端模块位置
```
backend/app/plugin/
├── module_application/
│   ├── ai/
│   ├── job/
│   ├── myapp/
│   └── workflow/
│
├── module_dataset/          ← 待调整
│   └── dataset/             ← 多余的嵌套层级
│       ├── __init__.py
│       ├── controller.py
│       ├── service.py
│       ├── crud.py
│       ├── model.py
│       ├── schema.py
│       ├── task.py
│       └── utils.py
│
├── module_train/            ← 待调整
│   └── train/               ← 多余的嵌套层级
│       ├── __init__.py
│       ├── controller.py
│       ├── service.py
│       ├── crud.py
│       ├── model.py
│       ├── schema.py
│       └── param.py
│
├── module_example/
└── module_generator/
```

### 前端模块位置
```
frontend/src/
├── api/
│   ├── module_train/        ← API调用 /train
│   │   └── model.ts
│   └── module_dataset/      ← API调用 /dataset/data
│       └── dataset.ts
│
└── views/
    └── module_dataset/
        ├── dataset/
        │   ├── index.vue
        │   └── components/
        └── robustness/
            ├── index.vue
            └── components/
                └── ModelRecommendation.vue
```

---

## 路由信息（预期）

### module_train 路由
**前缀:** `/train`
**路由数:** 15个

<details>
<summary>展开查看详细路由</summary>

1. POST `/train/recommend` - 模型推荐
2. GET `/train/model/detail/{id}` - 获取模型详情
3. GET `/train/model/list` - 分页查询模型列表
4. POST `/train/model/create` - 创建模型配置
5. PUT `/train/model/update/{id}` - 更新模型配置
6. DELETE `/train/model/delete` - 批量删除模型
7. GET `/train/model/statistics` - 获取统计信息
8. GET `/train/model/popular` - 获取热门模型
9. POST `/train/model/increment-usage/{id}` - 增加使用次数
10. PUT `/train/model/status/{id}` - 更新单个模型状态
11. PUT `/train/model/batch-status` - 批量更新状态
12. GET `/train/model/by-modality` - 按模态查询
13. GET `/train/model/by-task` - 按任务类型查询
14. GET `/train/model/by-framework` - 按框架查询
15. GET `/train/model/search-by-tag` - 按标签搜索

</details>

### module_dataset 路由
**前缀:** `/dataset/data`
**路由数:** 13个

<details>
<summary>展开查看详细路由</summary>

1. POST `/dataset/data/upload/init` - 上传初始化
2. POST `/dataset/data/upload/chunk` - 分片上传
3. POST `/dataset/data/upload/complete` - 完成上传
4. POST `/dataset/data/upload/resume` - 断点续传查询
5. GET `/dataset/data/detail/{id}` - 获取数据集详情
6. GET `/dataset/data/list` - 分页查询数据集
7. PUT `/dataset/data/update/{id}` - 更新数据集
8. DELETE `/dataset/data/delete` - 批量删除数据集
9. GET `/dataset/data/statistics` - 获取统计信息
10. POST `/dataset/data/analyze/{id}` - 启动数据集分析
11. GET `/dataset/data/analyze/status/{id}` - 查询分析状态
12. GET `/dataset/data/preview/{id}` - 数据集预览
13. (可能有其他路由)

</details>

---

## 环境信息

### Python环境
- **Python版本:** 3.7.9
- **注意:** Python 3.7不支持 `typing.Literal`，项目可能需要Python 3.8+

### 依赖环境
- **数据库:** MySQL
- **缓存:** Redis
- **框架:** FastAPI + SQLAlchemy

---

## 功能验证状态

由于Python版本问题，服务暂时无法启动。建议：

### 选项A：升级Python到3.8+
```bash
# 使用Python 3.8+重新创建虚拟环境
python3.8 -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
```

### 选项B：降级typing_extensions
```bash
# 如果必须使用Python 3.7
pip install typing-extensions
# 然后修改导入: from typing_extensions import Literal
```

### 选项C：继续架构调整（推荐）
由于架构调整不涉及Python版本相关的代码修改，可以继续执行批次1-5，调整完成后再解决Python版本问题。

---

## 测试检查清单

详见: [测试检查清单.md](测试检查清单.md)

---

## 批次0完成状态

- [x] ✅ 确认Git状态
- [x] ✅ 创建代码快照 (commit: d9c0478)
- [x] ✅ 创建数据库备份脚本
- [x] ✅ 创建测试检查清单
- [x] ✅ 记录基线信息
- [ ] ⚠️ 服务启动验证（Python版本问题待解决）

**结论：** 批次0准备工作已完成，可以继续执行批次1（创建标准目录结构）。

---

**记录人：** Claude Code
**最后更新：** 2026-01-22 10:10
